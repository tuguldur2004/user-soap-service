# user-soap-service-spring

SOAP authentication service

## Features

- `RegisterUser`
- `LoginUser`
- `ValidateToken`

## Run

```bash
mvn spring-boot:run
```

Runs on `http://localhost:3001/ws`.

## Environment

- `AUTH_DB_URL`
- `AUTH_DB_USER`
- `AUTH_DB_PASSWORD`
- `APP_JWT_SECRET`
